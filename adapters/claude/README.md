# Claude Code adapter

Start Claude Code in the repository root. `CLAUDE.md` imports canonical `AGENTS.md`.

Query and claim the active frontier through `scripts/lattice.py`, edit only the claimed role's paths, and record the result through the guarded CLI. Chat history and auto memory are neither evidence nor operational state. Use a fresh context for independent review.

For a file-upload-only Claude session, use the same scoped export produced for ChatGPT Work and require one revision-guarded state delta in return.
